echo "==============================="
echo "  running kelrond-display.cfg"
echo "==============================="
echo

// standard settings
net_graph                   0       // Disable net_graph - it is added to tab instead through the scripts config
cl_disablehtmlmotd          1       // Disable "Message of the Day" on joining custom servers

// Display info over every teammate
cl_teamid_overhead_always   2       // Always show full equipment on teammates through walls. Reduces team kills.. :grimacing:

// Smaller AUG scoped crosshair
cl_crosshaircolor_r         -1000
cl_crosshaircolor_g         0
cl_crosshaircolor_b         -1000