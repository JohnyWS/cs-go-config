echo "================================"
echo "  running kelrond-bindings.cfg"
echo "================================"
echo

// grenades
bind c  slot6   // (HE grenade)
bind f  slot7   // (flash)
bind t  slot8   // (smoke)
bind x  slot9   // (decoy)
bind v  slot10  // (molotov)